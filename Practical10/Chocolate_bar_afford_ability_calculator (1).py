def f(money, price):
    y = money // price
# Give the function of the chocolate bar number
    z = money % price
# Give the function of the charge
    print("The chocolate bar number and the charge are")
    return [y, z]

# Use an example
money = 100
price = 7
print(f(money, price))
